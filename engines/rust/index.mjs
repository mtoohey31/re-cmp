class Regex{constructor(regex){this.inner=regex}inner;matches(text){return this.inner.matches(text)}drop(){this.inner.free()}}class Engine{constructor(engine){this.inner=engine}inner;compile(regex){const regex_=this.inner.compile(regex);return new Regex(regex_)}drop(){this.inner.free()}}const{default:init,Engine:Engine_}=await import("./re_cmp_engine_rust.js");await init();const engine_=Engine_.new();export const engine=new Engine(engine_);