import{Match}from"../index.mjs";class Regex{constructor(regex){this.inner=RegExp(regex,"g")}inner;matches(text){const matchArrayIter=text.matchAll(this.inner);const res=[];for(const matchArray of matchArrayIter){if(matchArray.index===undefined){continue}res.push(new Match(matchArray.index,matchArray.index+matchArray[0].length))}return res}drop(){}}class Engine{compile(regex){return new Regex(regex)}drop(){}}export const engine=new Engine;